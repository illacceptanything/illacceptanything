var accessibility_developer_tools = require("accessibility-developer-tools"),
	adm_zip = require("adm-zip"),
	ansi_styles = require("ansi-styles"),
	archiver = require("archiver"),
	array_differ = require("array-differ"),
	array_union = require("array-union"),
	assert = require("assert"),
	async = require("async"),
	aws_sdk = require("aws-sdk"),
	aws_sign2 = require("aws-sign2"),
	basic_auth_connect = require("basic-auth-connect"),
	benchmark = require("benchmark"),
	binary = require("binary"),
	bluebird = require("bluebird"),
	body_parser = require("body-parser"),
	bonsai = require("bonsai"),
	bower = require("bower"),
	browserify = require("browserify"),
	browserstack = require("browserstack"),
	browserstacktunnel_wrapper = require("browserstacktunnel-wrapper"),
	buffer = require("buffer"),
	buffer_crc32 = require("buffer-crc32"),
	bytes = require("bytes"),
	camel_case = require("camel-case"),
	canonical_path = require("canonical-path"),
	catharsis = require("catharsis"),
	chalk = require("chalk"),
	change_case = require("change-case"),
	cheerio = require("cheerio"),
	child_process = require("child_process"),
	chokidar = require("chokidar"),
	cli = require("cli"),
	clone = require("clone"),
	clone_stats = require("clone-stats"),
	cluster = require("cluster"),
	coffee_script_redux = require("coffee-script-redux"),
	collections = require("collections"),
	colors = require("colors"),
	commander = require("commander"),
	compound = require("compound"),
	compression = require("compression"),
	concat_with_sourcemaps = require("concat-with-sourcemaps"),
	connect = require("connect"),
	connect_livereload = require("connect-livereload"),
	connect_timeout = require("connect-timeout"),
	console = require("console"),
	console_browserify = require("console-browserify"),
	constant_case = require("constant-case"),
	convert_source_map = require("convert-source-map"),
	cookie = require("cookie"),
	cookie_parser = require("cookie-parser"),
	cookie_signature = require("cookie-signature"),
	cordova = require("cordova"),
	crypto = require("crypto"),
	csurf = require("csurf"),
	cycle = require("cycle"),
	datavjs = require("datavjs"),
	dateformat = require("dateformat"),
	debug = require("debug"),
	deepmerge = require("deepmerge"),
	dependency_graph = require("dependency-graph"),
	dgeni = require("dgeni"),
	dgeni_packages = require("dgeni-packages"),
	dgram = require("dgram"),
	di = require("di"),
	diff = require("diff"),
	dns = require("dns"),
	domain = require("domain"),
	domelementtype = require("domelementtype"),
	domhandler = require("domhandler"),
	domutils = require("domutils"),
	dot_case = require("dot-case"),
	duplexer = require("duplexer"),
	entities = require("entities"),
	errorhandler = require("errorhandler"),
	escape_html = require("escape-html"),
	escape_string_regexp = require("escape-string-regexp"),
	esprima = require("esprima"),
	esprima_harmony_jscs_regex_token_fix = require("esprima-harmony-jscs-regex-token-fix"),
	estraverse = require("estraverse"),
	event_stream = require("event-stream"),
	events = require("events"),
	exit = require("exit"),
	express = require("express"),
	express_session = require("express-session"),
	eyes = require("eyes"),
	fileset = require("fileset"),
	findup_sync = require("findup-sync"),
	first_chunk_stream = require("first-chunk-stream"),
	forever = require("forever"),
	forever_agent = require("forever-agent"),
	form_data = require("form-data"),
	formatio = require("formatio"),
	fresh = require("fresh"),
	from = require("from"),
	fs = require("fs"),
	fs_extra = require("fs-extra"),
	fstream = require("fstream"),
	gaze = require("gaze"),
	glob = require("glob"),
	graceful_fs = require("graceful-fs"),
	growl = require("growl"),
	grunt = require("grunt"),
	grunt_bump = require("grunt-bump"),
	grunt_cli = require("grunt-cli"),
	grunt_contrib_clean = require("grunt-contrib-clean"),
	grunt_contrib_compress = require("grunt-contrib-compress"),
	grunt_contrib_connect = require("grunt-contrib-connect"),
	grunt_contrib_copy = require("grunt-contrib-copy"),
	grunt_contrib_jshint = require("grunt-contrib-jshint"),
	grunt_ddescribe_iit = require("grunt-ddescribe-iit"),
	grunt_jasmine_node = require("grunt-jasmine-node"),
	grunt_jscs = require("grunt-jscs"),
	grunt_merge_conflict = require("grunt-merge-conflict"),
	grunt_shell = require("grunt-shell"),
	gulp = require("gulp"),
	gulp_concat = require("gulp-concat"),
	gulp_foreach = require("gulp-foreach"),
	gulp_jshint = require("gulp-jshint"),
	gulp_rename = require("gulp-rename"),
	gulp_sourcemaps = require("gulp-sourcemaps"),
	gulp_uglify = require("gulp-uglify"),
	gulp_util = require("gulp-util"),
	handlebars = require("handlebars"),
	has_ansi = require("has-ansi"),
	hawk = require("hawk"),
	holler = require("holler"),
	hooker = require("hooker"),
	html_entities = require("html-entities"),
	htmlparser2 = require("htmlparser2"),
	http = require("http"),
	http_signature = require("http-signature"),
	https = require("https"),
	inherits = require("inherits"),
	iodocs = require("iodocs"),
	is_lower_case = require("is-lower-case"),
	is_upper_case = require("is-upper-case"),
	is_utf8 = require("is-utf8"),
	jade = require("jade"),
	jasmine = require("jasmine"),
	jasmine_core = require("jasmine-core"),
	jasmine_growl_reporter = require("jasmine-growl-reporter"),
	jasmine_node = require("jasmine-node"),
	jasmine_reporters = require("jasmine-reporters"),
	jasminewd = require("jasminewd"),
	jasminewd2 = require("jasminewd2"),
	jscs = require("jscs"),
	jsdom = require("jsdom"),
	jshint = require("jshint"),
	jshint_stylish = require("jshint-stylish"),
	json_stringify_safe = require("json-stringify-safe"),
	karma = require("karma"),
	karma_browserstack_launcher = require("karma-browserstack-launcher"),
	karma_chrome_launcher = require("karma-chrome-launcher"),
	karma_firefox_launcher = require("karma-firefox-launcher"),
	karma_jasmine = require("karma-jasmine"),
	karma_junit_reporter = require("karma-junit-reporter"),
	karma_ng_scenario = require("karma-ng-scenario"),
	karma_sauce_launcher = require("karma-sauce-launcher"),
	karma_script_launcher = require("karma-script-launcher"),
	lazystream = require("lazystream"),
	less = require("less"),
	load_grunt_tasks = require("load-grunt-tasks"),
	lodash = require("lodash"),
	lodash._baseclone = require("lodash._baseclone"),
	lodash._basecreatecallback = require("lodash._basecreatecallback"),
	lodash._reinterpolate = require("lodash._reinterpolate"),
	lodash.clone = require("lodash.clone"),
	lodash.template = require("lodash.template"),
	log_symbols = require("log-symbols"),
	lolex = require("lolex"),
	lower_case = require("lower-case"),
	lower_case_first = require("lower-case-first"),
	lru_cache = require("lru-cache"),
	map_stream = require("map-stream"),
	marked = require("marked"),
	match_stream = require("match-stream"),
	method_override = require("method-override"),
	mime = require("mime"),
	mimeparse = require("mimeparse"),
	minijasminenode = require("minijasminenode"),
	minimatch = require("minimatch"),
	minimist = require("minimist"),
	mkdirp = require("mkdirp"),
	mocha = require("mocha"),
	moment = require("moment"),
	mongodb = require("mongodb"),
	mongoose = require("mongoose"),
	morgan = require("morgan"),
	multimatch = require("multimatch"),
	multiparty = require("multiparty"),
	multipipe = require("multipipe"),
	net = require("net"),
	node_html_encoder = require("node-html-encoder"),
	node_uuid = require("node-uuid"),
	nodeunit = require("nodeunit"),
	npm = require("npm"),
	numbers = require("numbers"),
	nunjucks = require("nunjucks"),
	oauth_sign = require("oauth-sign"),
	on_headers = require("on-headers"),
	open = require("open"),
	optimist = require("optimist"),
	os = require("os"),
	param_case = require("param-case"),
	parseurl = require("parseurl"),
	pascal_case = require("pascal-case"),
	path = require("path"),
	path_case = require("path-case"),
	pause = require("pause"),
	pause_stream = require("pause-stream"),
	pkgcloud = require("pkgcloud"),
	pkginfo = require("pkginfo"),
	pm2 = require("pm2"),
	portscanner = require("portscanner"),
	prettysize = require("prettysize"),
	promises_aplus_tests = require("promises-aplus-tests"),
	protractor = require("protractor"),
	pullstream = require("pullstream"),
	punycode = require("punycode"),
	q = require("q"),
	q_io = require("q-io"),
	qq = require("qq"),
	qs = require("qs"),
	querystring = require("querystring"),
	readable_stream = require("readable-stream"),
	readline = require("readline"),
	redis = require("redis"),
	repl = require("repl"),
	request = require("request"),
	requirejs = require("requirejs"),
	response_time = require("response-time"),
	rethinkdb = require("rethinkdb"),
	rewire = require("rewire"),
	rimraf = require("rimraf"),
	sauce_connect_launcher = require("sauce-connect-launcher"),
	saucelabs = require("saucelabs"),
	selenium_webdriver = require("selenium-webdriver"),
	semver = require("semver"),
	sentence_case = require("sentence-case"),
	serve_favicon = require("serve-favicon"),
	serve_index = require("serve-index"),
	serve_static = require("serve-static"),
	setimmediate = require("setimmediate"),
	shelljs = require("shelljs"),
	should = require("should"),
	sigmund = require("sigmund"),
	sinon = require("sinon"),
	smalloc = require("smalloc"),
	snake_case = require("snake-case"),
	socket.io = require("socket.io"),
	socket.io_client = require("socket.io-client"),
	sorted_object = require("sorted-object"),
	source_map = require("source-map"),
	source_map_support = require("source-map-support"),
	split = require("split"),
	stack_trace = require("stack-trace"),
	statsd = require("statsd"),
	stream = require("stream"),
	stream_combiner = require("stream-combiner"),
	string_decoder = require("string_decoder"),
	string_length = require("string-length"),
	stringmap = require("stringmap"),
	strip_ansi = require("strip-ansi"),
	strip_bom = require("strip-bom"),
	strip_json_comments = require("strip-json-comments"),
	stylus = require("stylus"),
	supports_color = require("supports-color"),
	swap_case = require("swap-case"),
	tape = require("tape"),
	tar_stream = require("tar-stream"),
	text_table = require("text-table"),
	three = require("three"),
	through = require("through"),
	through2 = require("through2"),
	title_case = require("title-case"),
	tls = require("tls"),
	tmp = require("tmp"),
	topojson = require("topojson"),
	tough_cookie = require("tough-cookie"),
	tty = require("tty"),
	tunnel_agent = require("tunnel-agent"),
	type_is = require("type-is"),
	uglify_js = require("uglify-js"),
	uglify_to_browserify = require("uglify-to-browserify"),
	underscore = require("underscore"),
	underscore.string = require("underscore.string"),
	underscore_contrib = require("underscore-contrib"),
	unzip = require("unzip"),
	upper_case = require("upper-case"),
	upper_case_first = require("upper-case-first"),
	url = require("url"),
	url2 = require("url2"),
	util = require("util"),
	util = require("util"),
	validate.js = require("validate.js"),
	vargs = require("vargs"),
	vhost = require("vhost"),
	vinyl = require("vinyl"),
	vinyl_sourcemaps_apply = require("vinyl-sourcemaps-apply"),
	vm = require("vm"),
	vow = require("vow"),
	vow_fs = require("vow-fs"),
	walkdir = require("walkdir"),
	wd = require("wd"),
	weak_map = require("weak-map"),
	win_spawn = require("win-spawn"),
	winston = require("winston"),
	ws = require("ws"),
	xcharts = require("xcharts"),
	xml2js = require("xml2js"),
	xmlbuilder = require("xmlbuilder"),
	yeoman_generator = require("yeoman-generator"),
	yo = require("yo"),
	zeroclipboard = require("zeroclipboard"),
	zip_stream = require("zip-stream"),
	zlib = require("zlib");

console.log("insert app.js here, be sure to include all the js libraries ever.");
// TODO: write the rest of app.js
