YUI.add('test_mojit_1Bindertest_1', function(Y, NAME) {

    Y.namespace('mojito.binders')[NAME] = {

        init: function(mojitProxy) {
        },

        bind: function(node) {
        }

    };

}, '0.0.1', {requires: ['mojito-client']});
